---
import Layout from "@/astro/layout/layout.astro"
import { StaticPage } from "@/components/layout/static-page"
import { t } from "@/utils/lang"

Astro.params.path = "success"
---

<Layout title={t("funnel:success")} lang="en">
  <StaticPage title={t("funnel:success")}>
    <div class="max-w-screen-md text-lg">{t("funnel:registrationConfirmed")}</div>
  </StaticPage>
</Layout>
